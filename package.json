{
  "name": "friendshiii-engine",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run src/api.ts --watch",
    "db:generate": "bun x drizzle-kit generate",
    "db:migrate": "bun x drizzle-kit migrate",
    "db:seed": "bun run src/db/seed.ts",
    "db:push": "bun x drizzle-kit push"
  },
  "dependencies": {
    "drizzle-orm": "^0.37.0",
    "drizzle-zod": "^0.5.1",
    "hono": "^4.6.12",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@libsql/client": "^0.14.0",
    "@types/bun": "latest",
    "drizzle-kit": "^0.29.1"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}